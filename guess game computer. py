#the user guess number of computer by using this code. 
import random #pakege for selection of random value. 


def guess(x):#create a guess funtion. 
    random_number =random.randint(1,x)
    guess=0
    
    while guess!= random_number:#To create loop while running this game. 
        guess = int(input(f'guess a number between 1 and {x}:'))
        
        if guess < random_number:
            print('Sorry, guess again,guess value too low.')
        elif guess > random_number:
            print('Sorry, guess again,guess value too high.')
    print(f'Congrats.You have guessed the number{random_number} correctly') 
    
guess(10)#for selecting final value. 
